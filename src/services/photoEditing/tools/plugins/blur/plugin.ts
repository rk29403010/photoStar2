import type { PhotoEditToolPlugin } from '../../../photoEditToolPlugin.ts'; import { renderBlur } from './implementation.ts'; import { blurBrowserPreview } from '../../browserPreview.ts';
export const blurPlugin: PhotoEditToolPlugin = { id: 'blur', recipeVersion: 1, label: 'Blur', icon: 'Blend', group: 'filter', defaults: { sigma: 2 }, controls: [{ key: 'sigma', label: 'Strength', min: 0.3, max: 30, step: 0.1 }], browserPreview: blurBrowserPreview, renderExact: renderBlur, capabilities: { maskCompatible: true }, help: { description: 'Softens detail.', accessibilityLabel: 'Add blur' }, errorBoundaryDisplayName: 'Blur' };
