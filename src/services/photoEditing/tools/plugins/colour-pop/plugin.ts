import type { PhotoEditToolPlugin } from '../../../photoEditToolPlugin.ts'; import { applyColourPop } from '../../../editRenderer.ts';
export const colourPopPlugin: PhotoEditToolPlugin = { id: 'colour_pop', recipeVersion: 1, label: 'Colour pop', icon: 'Palette', group: 'effects', defaults: { colourCount: 0, colourRange: 28, softness: 0.35 }, renderExact: (input, operation) => applyColourPop(input, operation), capabilities: { maskCompatible: true, requiresSourceImage: true }, help: { description: 'Retains selected colours.', accessibilityLabel: 'Add colour pop' }, errorBoundaryDisplayName: 'Colour pop' };
