import { frameCropBox } from '../../../../../shared/photoEditing/automatic.ts';
import { automaticContextFromAsset } from '../../automaticContext.ts';
import type { PhotoEditToolSuggestionPlugin } from '../../../../../ui/components/photo-editor/photoEditToolSuggestionRegistry.ts';
const plugin: PhotoEditToolSuggestionPlugin = { id: 'crop', suggestAutomatic: ({ analysis, asset, semanticGeometrySafe }) => { const context = automaticContextFromAsset(asset); const safe = semanticGeometrySafe ? context : { ...context, attentionBoxes: [], faceBoxes: [], frameBox: null }; const detectedFrame = frameCropBox(safe); const box = detectedFrame ?? analysis.attentionCrop; return !box || box.width * box.height > 0.94 ? null : { confidence: detectedFrame ? 0.98 : Math.min(0.82, analysis.confidence), id: detectedFrame ? 'automatic-frame-crop' : 'automatic-attention-crop', label: detectedFrame ? 'Remove detected frame' : 'Crop around the subject', name: detectedFrame ? 'Remove detected frame' : 'Smart subject crop', rationale: detectedFrame ? 'Uses the photo boundary saved by the ingest workflow.' : 'Keeps detected faces and important regions with comfortable breathing room.', tool: 'crop', values: { ...box } }; } };
export default plugin;
