import type { PhotoEditToolPlugin } from '../../../photoEditToolPlugin.ts'; import { applyDehaze } from '../../../editRenderer.ts'; import { dehazeBrowserPreview } from '../../browserPreview.ts';
export const dehazePlugin: PhotoEditToolPlugin = { id: 'dehaze', recipeVersion: 1, label: 'Dehaze', icon: 'CloudSun', group: 'tone', defaults: { strength: 0.45, radiusPercent: 1.5 }, controls: [{ key: 'strength', label: 'Fog removal', min: 0, max: 1, step: 0.01 }, { key: 'radiusPercent', label: 'Detail radius', min: 0.5, max: 3, step: 0.25 }], browserPreview: dehazeBrowserPreview, renderExact: (input, operation) => applyDehaze(input, operation), capabilities: { maskCompatible: true }, help: { description: 'Recovers contrast through haze.', accessibilityLabel: 'Add dehaze' }, errorBoundaryDisplayName: 'Dehaze' };
