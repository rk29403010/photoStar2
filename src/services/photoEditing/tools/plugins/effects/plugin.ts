import type { PhotoEditToolPlugin } from '../../../photoEditToolPlugin.ts'; import { renderEffects } from './implementation.ts'; import { PHOTO_EFFECT_DEFAULTS, PHOTO_EFFECT_KIND } from '../../../../../shared/photoEditing/effects.ts';
export const effectsPlugin: PhotoEditToolPlugin = { id: 'effects', recipeVersion: 1, label: 'Effects', icon: 'Sparkles', group: 'effects', defaults: PHOTO_EFFECT_DEFAULTS[PHOTO_EFFECT_KIND.ripple], renderExact: renderEffects, capabilities: { maskCompatible: true }, help: { description: 'Adds deterministic visual effects.', accessibilityLabel: 'Add effects' }, errorBoundaryDisplayName: 'Effects' };
