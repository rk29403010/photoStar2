import type { PhotoEditToolPlugin } from '../../../photoEditToolPlugin.ts'; import { renderFocus } from './implementation.ts'; import { FOCUS_DEFAULTS } from '../../../../../shared/photoEditing/focus.ts';
export const focusPlugin: PhotoEditToolPlugin = { id: 'focus', recipeVersion: 1, label: 'Focus', icon: 'Aperture', group: 'effects', defaults: FOCUS_DEFAULTS, renderExact: renderFocus, capabilities: { maskCompatible: true }, help: { description: 'Creates focal blur zones.', accessibilityLabel: 'Add focus' }, errorBoundaryDisplayName: 'Focus' };
