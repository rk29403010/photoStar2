import type { PhotoEditToolPlugin } from '../../../photoEditToolPlugin.ts'; import { renderRedEye } from './implementation.ts'; import { RED_EYE_DEFAULTS } from '../../../../../shared/photoEditing/redEye.ts';
export const redEyePlugin: PhotoEditToolPlugin = { id: 'red_eye', recipeVersion: 1, label: 'Red eye', icon: 'ScanEye', group: 'restore', defaults: RED_EYE_DEFAULTS, renderExact: renderRedEye, capabilities: { maskCompatible: true, requiresSourceImage: true, requiresAssetMetadata: true }, help: { description: 'Corrects detected eye reflections.', accessibilityLabel: 'Add red eye correction' }, errorBoundaryDisplayName: 'Red eye' };
