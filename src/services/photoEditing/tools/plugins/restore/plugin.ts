import type { PhotoEditToolPlugin } from '../../../photoEditToolPlugin.ts'; import { applyRestoreExact } from '../../../editRenderer.ts'; import { restoreBrowserPreview } from '../../browserPreview.ts';
export const restorePlugin: PhotoEditToolPlugin = { id: 'restore', recipeVersion: 1, label: 'Restore old photo', icon: 'ImageUp', group: 'restore', defaults: { fadeRecovery: 1.08, contrast: 0.12, saturation: 1.08, denoise: 1, detail: 0.8 }, controls: [{ key: 'fadeRecovery', label: 'Fade recovery', min: 0.5, max: 2, step: 0.01 }, { key: 'contrast', label: 'Contrast', min: -0.5, max: 1, step: 0.01 }, { key: 'saturation', label: 'Colour recovery', min: 0, max: 2, step: 0.01 }, { key: 'denoise', label: 'Dust reduction', min: 1, max: 5, step: 2 }, { key: 'detail', label: 'Detail recovery', min: 0.1, max: 5, step: 0.1 }], browserPreview: restoreBrowserPreview, renderExact: (input, operation) => applyRestoreExact(input, operation), capabilities: { maskCompatible: true, requiresSourceImage: true }, help: { description: 'Recovers aged images.', accessibilityLabel: 'Add restoration' }, errorBoundaryDisplayName: 'Restore old photo' };
