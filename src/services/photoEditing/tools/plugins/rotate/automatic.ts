import type { PhotoEditToolSuggestionPlugin } from '../../../../../ui/components/photo-editor/photoEditToolSuggestionRegistry.ts';
const plugin: PhotoEditToolSuggestionPlugin = { id: 'rotate', suggestAutomatic: ({ analysis }) => analysis.straightenAngle === 0 || analysis.straightenConfidence < 0.28 ? null : { confidence: analysis.straightenConfidence, id: 'automatic-straighten', label: `Straighten ${Math.abs(analysis.straightenAngle).toFixed(1)}°`, name: 'Automatic straighten', rationale: 'Aligns the strongest repeated horizontal or vertical lines. EXIF orientation is already handled separately.', tool: 'rotate', values: { angle: analysis.straightenAngle, pivotX: 0.5, pivotY: 0.5, expandCanvas: false, fillMode: 0, flipHorizontal: false, flipVertical: false } } };
export default plugin;
