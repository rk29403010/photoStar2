import type { PhotoEditToolPlugin } from '../../../photoEditToolPlugin.ts'; import { renderSharpen } from './implementation.ts'; import { sharpenBrowserPreview } from '../../browserPreview.ts';
export const sharpenPlugin: PhotoEditToolPlugin = { id: 'sharpen', recipeVersion: 1, label: 'Details', icon: 'Focus', group: 'filter', defaults: { sigma: 1 }, controls: [{ key: 'sigma', label: 'Strength', min: 0.1, max: 5, step: 0.1 }], browserPreview: sharpenBrowserPreview, renderExact: renderSharpen, capabilities: { maskCompatible: true }, help: { description: 'Enhances local detail.', accessibilityLabel: 'Add detail enhancement' }, errorBoundaryDisplayName: 'Details' };
